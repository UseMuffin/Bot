# Muffin Bot


## Install

```
composer create-project -s dev --prefer-dist muffin/bot
```

## Usage

Start by configuring it. For now, everything is in `config.php`.

Once done, run:

```
vendor/bin/phergie
```
